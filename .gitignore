# Python
detector/.venv/
__pycache__/
*.pyc
.python-version

# Models (downloaded, not committed)
models/*.task

# macOS
.DS_Store

# Max
*.maxsnapshot
*.amxd.bak

# superseded scratch device (early jsui+umenu experiment)
device/Gestures.amxd

# user-specific camera choice
detector/settings.json

# PyInstaller build artifacts (the .app ships via GitHub Releases, not git)
build/
dist/
*.spec
*.log
